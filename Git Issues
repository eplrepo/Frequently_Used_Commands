http://www.thisprogrammingthing.com/2013/fixing-the-this-exceeds-githubs-file-size-limit-of-100-mb-error/

git filter-branch --index-filter 'git rm --cached --ignore-unmatch output'

git filter-branch --index-filter 'git rm --cached --ignore-unmatch LTPbuildroot/output/images/Pi2/rootfs.cpio'

1. git lfs install

2. git lfs track "*.zip"
git lfs track "*.ext2"
git lfs track "*.ext4"
git lfs track "*.img"
git lfs track "*.tar.gz"
git lfs track "*.cpio"
git lfs track "*.tar.xz"
git lfs track "*.gch"


3. git lfs track

4. git status

5. git add .gitattributes

6. git add -f *

7. git commit -m "Initial commit 7"

8. git push



git diff > some-changes.patch

git apply /path/to/some-changes.patch

git am --signoff < a_file.patch 

